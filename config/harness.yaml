workspace:
  root: ./workspace

data:
  root: ./data

skills:
  root: ./skills

browser:
  profile_path: ./data/browser-profile
  headless: false

agent:
  max_steps: 50
  max_consecutive_errors: 5
  max_tool_runtime_seconds: 300

context:
  max_messages: 40
  keep_recent: 16
